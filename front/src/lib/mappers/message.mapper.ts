/**
 * Mapper de messages — transforme les payloads bruts socket en objets Message typés.
 * Importé par les handlers socket (WebSocketEvents) et les stores.
 */
export { transformToMessage } from '@/types/chat';
