/**
 * Mapper de conversations — transforme les payloads bruts socket en objets Conversation typés.
 * Importé par les handlers socket (WebSocketEvents) et les stores.
 */
export { transformToConversation } from '@/types/chat';
