/** Nom de la connexion TypeORM vers la base de commandes (DB2). */
export const ORDER_DB_CONNECTION = 'order-db';

/** Token d'injection du DataSource DB2 (peut être null si non configuré). */
export const ORDER_DB_DATA_SOURCE = 'ORDER_DB_DATA_SOURCE';

/** Token indiquant si la connexion DB2 est disponible (boolean). */
export const ORDER_DB_AVAILABLE = 'ORDER_DB_AVAILABLE';
