/**
 * TICKET-09-C — Re-export de ChannelsView depuis ui/.
 * Transition : le fichier source reste dans ui/ pendant la migration.
 * Cleanup : TICKET-09-C-CLEANUP supprimera ui/ChannelsView.tsx et déplacera le contenu ici.
 */
export { default } from '@/app/ui/ChannelsView';
