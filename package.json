{
  "name": "whatsapp-dispatcher-monorepo",
  "private": true,
  "scripts": {
    "dev:back": "npm run start:dev --prefix message_whatsapp",
    "dev:front": "npm run dev --prefix front",
    "dev:admin": "npm run dev --prefix admin",
    "install:all": "npm install && npm install --prefix message_whatsapp && npm install --prefix front && npm install --prefix admin",
    "check:socket-sync": "node scripts/check-socket-events-sync.js",
    "test:all": "npm run check:socket-sync && npm test --prefix front"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
